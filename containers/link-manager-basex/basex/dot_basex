# BaseX options for the DFST Link Management 
# application. Sets up BaseX to enable import
# of DTD-based DITA documents. Requires
# a properly-configured DITA Open Toolkit to provide
# XML catalog resolution.
# NOTE: the CATFILE location depends on runnning
#       BaseX container with volumes from the dita-ot
#       container, e.g., run --volumes-from dfst/dita-ot ...
# Local options
CATFILE = /opt/dita-ot/DITA-OT/catalog-dita.xml
DTD = true
SKIPCORRUPT = true
CHOP = false